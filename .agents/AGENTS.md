# Agent Rules

## TypeScript & Code Quality
- Avoid using the `any` type in TypeScript whenever possible. Prefer specific interfaces/types, generics, `unknown`, or proper union types instead.
- Specify explicit return types on top-level functions and public class methods.
- Always run `npm run typecheck` or relevant tests after modifying code.

